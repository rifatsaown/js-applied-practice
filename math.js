// random number
const myNumber = Math.random()*-6;
const Number = Math.round(myNumber);
console.log(Number);
// for loop
for (var i = 1; i <= 2; i++) {
    const myNumber = Math.random() * 6;
    const Number = Math.floor(myNumber);
    console.log(Number);
}
//absNumber
const absNumber = Math.abs(Number);
console.log(absNumber);
